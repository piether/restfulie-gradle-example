apply plugin: 'war'
apply plugin: 'jetty'

repositories {
       mavenRepo name: 'releases', urls: "http://localhost:8081/artifactory/libs-releases";
}

group = 'be.piether'
version = '1.0'

dependencies {
    compile 'br.com.caelum:vraptor:3.3.1',
            'com.thoughtworks.xstream:xstream:1.3.1'

}
